# Настройка окружения разработки

## 🎯 Варианты разработки

### 1. Remix IDE (Рекомендуется для начинающих)

**Онлайн IDE** - не требует установки!

**Ссылка**: https://remix.ethereum.org/

**Преимущества**:
- ✅ Работает в браузере
- ✅ Не нужна установка
- ✅ Встроенный компилятор
- ✅ Деплой в тестовые сети
- ✅ Отладчик

**Как начать**:
1. Открой https://remix.ethereum.org/
2. Создай новый файл `.sol`
3. Пиши код и компилируй
4. Деплой и тестируй

### 2. Локальная разработка (Hardhat)

**Для серьезных проектов**

## 🛠️ Установка Hardhat

### Шаг 1: Установка Node.js

**Windows**:
```bash
# Скачай с https://nodejs.org/
# Установи LTS версию (v18+)

# Проверка установки
node --version
npm --version
```

### Шаг 2: Создание проекта

```bash
# Создай папку проекта
mkdir my-solidity-project
cd my-solidity-project

# Инициализация npm
npm init -y

# Установка Hardhat
npm install --save-dev hardhat
```

### Шаг 3: Инициализация Hardhat

```bash
# Запуск мастера настройки
npx hardhat

# Выбери: Create a JavaScript project
# Согласись с настройками по умолчанию
```

### Шаг 4: Установка зависимостей

```bash
# OpenZeppelin - библиотеки контрактов
npm install @openzeppelin/contracts

# Chai - для тестирования
npm install --save-dev chai

# Ethers.js - для взаимодействия с Ethereum
npm install --save-dev @nomiclabs/hardhat-ethers ethers
```

## 📁 Структура проекта Hardhat

```
my-solidity-project/
├── contracts/          # Смарт-контракты (.sol)
├── scripts/            # Скрипты деплоя
├── test/              # Тесты
├── hardhat.config.js  # Конфигурация
└── package.json       # Зависимости
```

## 🔧 VS Code настройка

### Установка расширений

1. **Solidity** by Juan Blanco
   - Подсветка синтаксиса
   - Автодополнение
   - Проверка ошибок

2. **Hardhat Solidity** by Nomic Foundation
   - Интеграция с Hardhat

### Настройка VS Code

Создай `.vscode/settings.json`:

```json
{
  "solidity.compileUsingRemoteVersion": "v0.8.19",
  "solidity.defaultCompiler": "remote",
  "editor.formatOnSave": true
}
```

## 🦊 MetaMask

**Кошелек для работы с DApps**

### Установка

1. Открой https://metamask.io/
2. Скачай расширение для браузера
3. Создай новый кошелек
4. **СОХРАНИ seed-фразу!**

### Добавление тестовой сети

**Sepolia Testnet** (рекомендуется):

```
Название сети: Sepolia
RPC URL: https://rpc.sepolia.org
Chain ID: 11155111
Символ: SepoliaETH
Block Explorer: https://sepolia.etherscan.io
```

### Получение тестового эфира

**Faucets** (краны для получения бесплатного тестового ETH):

- https://sepoliafaucet.com/
- https://faucet.sepolia.dev/
- https://www.alchemy.com/faucets/ethereum-sepolia

## 🧪 Тестовые сети

| Сеть | Назначение | Получить ETH |
|------|------------|--------------|
| **Sepolia** | Основная тестовая сеть | Faucets |
| **Goerli** | Альтернативная | Faucets |
| **Mumbai** | Polygon testnet | Faucets |
| **Hardhat** | Локальная сеть | Автоматически |

## 🚀 Проверка установки

### Создай тестовый контракт

`contracts/HelloWorld.sol`:

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract HelloWorld {
    string public message = "Hello, Hardhat!";
    
    function setMessage(string memory newMessage) public {
        message = newMessage;
    }
}
```

### Компиляция

```bash
npx hardhat compile
```

**Ожидаемый результат**:
```
Compiled 1 Solidity file successfully
```

### Запуск локальной сети

```bash
npx hardhat node
```

**Результат**: Локальный блокчейн на `http://127.0.0.1:8545/`

### Деплой контракта

Создай `scripts/deploy.js`:

```javascript
async function main() {
  const HelloWorld = await ethers.getContractFactory("HelloWorld");
  const hello = await HelloWorld.deploy();
  await hello.deployed();
  
  console.log("HelloWorld deployed to:", hello.address);
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
```

Запуск:

```bash
npx hardhat run scripts/deploy.js --network localhost
```

## 📚 Полезные команды Hardhat

```bash
# Компиляция
npx hardhat compile

# Запуск тестов
npx hardhat test

# Запуск локальной сети
npx hardhat node

# Деплой
npx hardhat run scripts/deploy.js --network <network-name>

# Очистка артефактов
npx hardhat clean

# Консоль
npx hardhat console
```

## 🌐 Альтернативные инструменты

### Truffle

```bash
npm install -g truffle
truffle init
```

### Foundry (Rust-based)

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
```

## ✅ Чек-лист готовности

- [ ] Node.js установлен
- [ ] Hardhat проект создан
- [ ] VS Code с расширениями настроен
- [ ] MetaMask установлен
- [ ] Тестовая сеть добавлена
- [ ] Тестовый ETH получен
- [ ] Первый контракт скомпилирован

## 🎯 Что дальше?

Теперь ты готов к разработке! Переходи к:

[Первый смарт-контракт](./03-Первый-контракт.md)

---

**Окружение настроено! Время писать код! 💻**
